
<!-- Start Shop Newsletter  -->
{{--<section class="shop-newsletter section">
    <div class="container">
        <div class="inner-top">
            <div class="row">
                <div class="col-lg-8 offset-lg-2 col-12">
                    <!-- Start Newsletter Inner -->
                    <div class="inner">
                        <h4>{{__('application.newsletter')}}</h4>
                        <p> {{__('application.subscribe_to_our_newsletter_and_get')}} <span>10%</span> {{__('application.off_your_first_purchase')}}</p>
                        <form action="{{route('subscribe')}}" method="post" class="newsletter-inner">
                            @csrf
                            <input name="email" placeholder="{{__('application.your_email_address')}}" required="" type="email">
                            <button class="btn" type="submit">{{__('application.subscribe')}}</button>
                        </form>
                    </div>
                    <!-- End Newsletter Inner -->
                </div>
            </div>
        </div>
    </div>
</section>--}}
<!-- End Shop Newsletter -->